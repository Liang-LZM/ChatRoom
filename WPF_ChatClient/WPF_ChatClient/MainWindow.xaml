<Window x:Class="WPF_ChatClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_ChatClient"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="9*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="聊天室" Grid.Row="0" FontSize="20" Margin="2" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <Grid Grid.Row="1" Background="#007834">
            <TextBlock Text="登录界面"  FontSize="20" Margin="2"   VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Grid>
        <Grid Grid.Row="3" HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="账号名" FontSize="20" Grid.Row="0" Grid.Column="0"/>
            <TextBox Text="{Binding LoginM.Id}" Height="20" Width="200" Grid.Row="0" Grid.Column="1"/>

            <TextBlock Text="密码" FontSize="20" Grid.Row="1" Grid.Column="0"/>
            <TextBox Text="{Binding LoginM.Password}" Height="20" Width="200" Grid.Row="1" Grid.Column="1"/>

            <CheckBox Content="显示密码" FontSize="15" Grid.Row="2" Grid.ColumnSpan="2"/>

            <Button Content="登录" FontSize="20" Grid.Row="3" Grid.ColumnSpan="2" Command="{Binding LoginAction}"/>

        </Grid>
    </Grid>
</Window>
